"use client"

import { Button } from "@/components/ui/button"

export const VerifyUser = ({ id }: { id: number }) => {
    return (
        <Button variant="default">
            Verify User
        </Button>
    )
}